  server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://${URL_BACK}:8080/api;
        #proxy_pass http://1.1.1.1:8080/;
    }
  }
